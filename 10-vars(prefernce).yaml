- name: variable prefrences
  hosts : local
  connection : local
  vars:
   - name : Deepthi via play
  vars_prompt:
   - name : name
     prompt : "what is your name:"
     private : No
  vars_files:
    - student.yaml
  tasks:
   - name : student info
     vars:
     - name : Deepthi via task
     ansible.builtin.debug:
      msg: " my name is: {{name}}"


