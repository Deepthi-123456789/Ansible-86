- name: variable prefrences
  hosts : localhost
  connection : local
  vars:
   - name : Gopi via play
  vars_prompt:
   - name : name
     prompt : "what is your name:"
     private : No
  vars_files:
    - student.yaml
  tasks:
   - name : student info
     vars:
     - name : Gopi via task
     ansible.builtin.debug:
      msg: " my name is: {{name}}"


