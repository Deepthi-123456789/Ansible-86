- name: variable via inventory file
  hosts: localhost
  tasks:
    - name: student info
      ansible.builtin.debug:
        msg: "my name is: {{ name }},age is: {{ age }},and course is: {{ course }}"